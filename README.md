O sistema utiliza um banco de dados MySQL local. Para configurar:

1. Edite o arquivo `db.properties` na raiz do projeto:
url=jdbc:mysql://localhost:3306/market
user=seu_usuario
password=sua_senha

2. Ao executar, o banco será criado automaticamente.
